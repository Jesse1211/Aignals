# Aignals
A free, open-source macOS menu bar indicator that visualizes AI coding agent activity in real time — red while a task is running, green when idle — driven by the agent's own   lifecycle hooks writing to a lightweight status file.
